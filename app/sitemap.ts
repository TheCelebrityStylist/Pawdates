import type {MetadataRoute} from 'next';import {posts} from '@/lib/posts';import {schedules} from '@/content/schedules';export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_APP_URL||'https://pawdates.app';return ['','/app','/blog','/schedules',...posts.map(p=>`/blog/${p.slug}`),...schedules.map(s=>`/schedules/${s.slug}`)].map(url=>({url:base+url,lastModified:new Date()}))}
