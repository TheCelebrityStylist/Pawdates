import Link from 'next/link';import {schedules} from '@/content/schedules';import {buildMetadata} from '@/lib/seo';export const metadata=buildMetadata({title:'Pet treatment schedules',description:'Veterinary-reference schedules for flea, tick, worming, vaccination, dental care, grooming and preventive check-ups.',path:'/schedules'});export default function Schedules(){return <main className="min-h-screen bg-[var(--paper)]"><section className="section pt-20"><div className="wrap"><p className="mono text-[var(--health)]">Reference library</p><h1 className="mt-4 text-5xl">Pet treatment schedules, made clear.</h1>{(['Dogs','Cats','Other'] as const).map(group=><section className="mt-12" key={group}><h2 className="border-b border-[var(--rule)] pb-3 text-3xl">{group}</h2><div className="mt-5 grid gap-4 md:grid-cols-2">{schedules.filter(item=>item.category===group).map(item=><Link className="card p-6" href={`/schedules/${item.slug}`} key={item.slug}><h3 className="text-xl">{item.query.replace(/^./,c=>c.toUpperCase())}</h3><p className="muted mt-2">{item.rows[2].interval}</p></Link>)}</div></section>)}</div></section></main>}
