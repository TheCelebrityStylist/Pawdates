import {ImageResponse} from 'next/og';import {post,posts} from '@/lib/posts';export const size={width:1200,height:630};export const contentType='image/png';export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))}export default async function Image({params}:{params:Promise<{slug:string}>}){const p=post((await params).slug);return new ImageResponse(<div style={{width:'100%',height:'100%',background:'#0F1210',color:'#F5F1EC',display:'flex',flexDirection:'column',justifyContent:'center',padding:80,fontFamily:'sans-serif'}}><div style={{color:'#C4633A',fontSize:28}}>PawDates journal</div><div style={{fontSize:70,lineHeight:1.08,maxWidth:1030,marginTop:36}}>{p?.title||'Pet care, clearly remembered.'}</div></div>)}
