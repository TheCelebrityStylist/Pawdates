import Link from 'next/link';import {posts} from '@/lib/posts';import {buildMetadata} from '@/lib/seo';export const revalidate=86400;export const metadata=buildMetadata({title:'Pet care guides',description:'Practical, veterinary-minded guides for flea treatment, worming, vaccination schedules and preventive pet care.',path:'/blog'});export default function Blog(){return <main className="min-h-screen bg-[var(--paper)]"><section className="section pt-20"><div className="wrap max-w-3xl"><p className="mono text-[var(--health)]">The Tailtend journal</p><h1 className="mt-4 text-5xl">Useful pet care, clearly remembered.</h1><div className="mt-14">{posts.map(post=><article key={post.slug} className="border-t border-[var(--rule)] py-8"><p className="mono text-[var(--ink-60)]">{new Date(post.date).toLocaleDateString('en-GB',{dateStyle:'long'})} · {post.minutes} min read</p><h2 className="mt-3 text-2xl"><Link href={`/blog/${post.slug}`}>{post.title}</Link></h2><p className="muted mt-2">{post.description}</p></article>)}</div></div></section></main>}
