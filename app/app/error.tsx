'use client';
import {useEffect} from 'react';import {Logo} from '@/components/logo';
export default function AppError({error,reset}:{error:Error&{digest?:string};reset:()=>void}){useEffect(()=>{console.error('[app] Dashboard render failed',error)},[error]);return <main className="grid min-h-screen place-items-center bg-[var(--paper)] px-5"><section className="card w-full max-w-lg p-8 text-center"><div className="flex justify-center"><Logo/></div><p className="mono mt-10 text-[var(--stamp)]">The record paused</p><h1 className="mt-3 text-4xl">Your dates are still safe.</h1><p className="muted mt-4">PawDates could not open the record just now. Try again, or return to the home page while the connection recovers.</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><button className="btn" onClick={reset}>Try again</button><a className="btn ghost" href="/">Back to PawDates</a></div></section></main>}
