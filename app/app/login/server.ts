'use server';import {supabase} from '@/lib/supabase';import {redirect} from 'next/navigation';export async function login(form:FormData){const email=String(form.get('email')||'');if(!/^\S+@\S+\.\S+$/.test(email))return;const s=await supabase();await s.auth.signInWithOtp({email,options:{emailRedirectTo:`${process.env.NEXT_PUBLIC_APP_URL}/auth/callback`}});redirect('/app/login/sent')}
