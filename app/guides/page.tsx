import Link from 'next/link';import {guides} from '@/content/guides';import {buildMetadata} from '@/lib/seo';export const metadata=buildMetadata({title:'Pet life-stage guides',description:'The whole first year for puppies and kittens, and what changes once a dog reaches senior care — laid out in order.',path:'/guides'});export default function Guides(){return <main className="min-h-screen bg-[var(--paper)]"><section className="section pt-20"><div className="wrap"><p className="mono text-[var(--health)]">Life-stage library</p><h1 className="mt-4 text-5xl">The whole stage, laid out in order.</h1><div className="mt-12 grid gap-4 md:grid-cols-2">{guides.map(item=><Link className="card p-6" href={`/guides/${item.slug}`} key={item.slug}><h3 className="text-xl">{item.title}</h3><p className="muted mt-2">{item.rows[0].what}</p></Link>)}</div></div></section></main>}
