import {ImageResponse} from 'next/og';import {guide,guides} from '@/content/guides';export const size={width:1200,height:630};export const contentType='image/png';export function generateStaticParams(){return guides.map(item=>({slug:item.slug}))}export default async function Image({params}:{params:Promise<{slug:string}>}){const item=guide((await params).slug);return new ImageResponse(<div style={{width:'100%',height:'100%',display:'flex',flexDirection:'column',justifyContent:'space-between',background:'#F7F2E9',color:'#22303C',padding:70,fontFamily:'sans-serif'}}><div style={{fontFamily:'monospace',fontSize:24,letterSpacing:3,textTransform:'uppercase',color:'#2F7D5B'}}>Tailtend · Life-stage guide</div><div style={{fontSize:72,fontWeight:800,lineHeight:1.05,maxWidth:1000}}>{item?.title||'Pet life-stage guide'}</div><div style={{display:'flex',justifyContent:'space-between',borderTop:'2px solid rgba(34,48,60,.18)',paddingTop:25,fontFamily:'monospace',fontSize:22}}><span>Reviewed against veterinary guidelines</span><span>{item?.updated}</span></div></div>)}
