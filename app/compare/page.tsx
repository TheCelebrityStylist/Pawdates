import Link from 'next/link';import {compares} from '@/content/compare';import {buildMetadata} from '@/lib/seo';export const metadata=buildMetadata({title:'Compare pet reminder tools',description:'Honest comparisons between Tailtend, generic calendars, and other pet medication reminder apps.',path:'/compare'});export default function Compare(){return <main className="min-h-screen bg-[var(--paper)]"><section className="section pt-20"><div className="wrap"><p className="mono text-[var(--health)]">Comparisons</p><h1 className="mt-4 text-5xl">Honest comparisons, not marketing copy.</h1><div className="mt-12 grid gap-4 md:grid-cols-2">{compares.map(item=><Link className="card p-6" href={`/compare/${item.slug}`} key={item.slug}><h3 className="text-xl">{item.title}</h3><p className="muted mt-2">{item.rows[0].feature}</p></Link>)}</div></div></section></main>}
