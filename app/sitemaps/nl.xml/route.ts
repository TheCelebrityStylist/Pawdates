import {dutchSchedules} from '@/content/schedules-nl';import {postsNl} from '@/content/blog-nl';import {xml,url} from '@/lib/sitemap';export function GET(){return xml(`<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${[url('/nl','2026-07-14'),url('/nl/blog','2026-07-19'),...dutchSchedules.map(item=>url(`/nl/schema/${item.slug}`,item.updated)),...postsNl.map(item=>url(`/nl/blog/${item.slug}`,item.updated))].join('')}</urlset>`)}
