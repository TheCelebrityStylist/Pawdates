import {redirect} from 'next/navigation';import {Landing} from '@/components/landing';import {baseGraph,buildMetadata,canonicalHost,graphJson} from '@/lib/seo';
export const metadata=buildMetadata({title:'Tailtend — pet treatment reminders that keep every date',description:'A calm treatment record for flea, worming, vaccinations and vet care, with reminders that keep every pet date.',path:''});
export default async function Page({searchParams}:{searchParams:Promise<{code?:string}>}){if((await searchParams).code)redirect('/app');const graph=[...baseGraph(''),{'@type':'SoftwareApplication','@id':`${canonicalHost}/#software`,name:'Tailtend',applicationCategory:'HealthApplication',operatingSystem:'iOS, Web',offers:[{'@type':'Offer',name:'Tailtend iOS',price:'4.99',priceCurrency:'EUR'},{'@type':'Offer',name:'Tailtend Web Premium',price:'9.99',priceCurrency:'EUR',priceSpecification:{'@type':'UnitPriceSpecification',billingDuration:'P1Y'}}]}];return <><Landing/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(graphJson(graph))}}/></>}
