import Link from 'next/link';import {dutchSchedules} from '@/content/schedules-nl';import {buildMetadata,baseGraph,graphJson} from '@/lib/seo';export const metadata=buildMetadata({title:'Verzorgingsschema’s voor hond en kat',description:'Duidelijke Nederlandse schema’s voor vlooien, ontwormen en vaccinaties van honden, katten, puppy’s en kittens.',path:'/nl'});export default function DutchHub(){return <main className="min-h-screen bg-[var(--paper)]"><section className="section pt-20"><div className="wrap"><p className="mono text-[var(--health)]">Nederlandse gidsen</p><h1 className="mt-4 text-5xl">Verzorgingsschema’s zonder gedoe.</h1><p className="muted mt-5 max-w-2xl">Zes praktische overzichten voor de behandelingen die het vaakst terugkomen.</p><div className="mt-12 grid gap-4 md:grid-cols-2">{dutchSchedules.map(item=><Link className="card p-6" href={`/nl/schema/${item.slug}`} key={item.slug}><h2 className="text-xl">{item.title}</h2><p className="muted mt-2">{item.intro}</p></Link>)}</div></div></section><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(graphJson(baseGraph('/nl')))}}/></main>}
