import Link from 'next/link';import {postsNl} from '@/content/blog-nl';import {buildMetadata} from '@/lib/seo';
export const metadata=buildMetadata({title:'Nederlandse gidsen — Tailtend blog',description:'Praktische Nederlandse artikelen over ontwormen, vlooienbehandeling en vaccinatieschema’s voor hond en kat.',path:'/nl/blog'});
export default function BlogNlIndex(){return <main className="min-h-screen bg-[var(--paper)]"><section className="section pt-20"><div className="wrap"><p className="mono text-[var(--health)]">Nederlandse gidsen</p><h1 className="mt-4 text-5xl">Achtergrond bij de schema’s.</h1><div className="mt-12 grid gap-4 md:grid-cols-2">{postsNl.map(item=><Link className="card p-6" href={`/nl/blog/${item.slug}`} key={item.slug}><h2 className="text-xl">{item.title}</h2><p className="muted mt-2">{item.intro.slice(0,120)}…</p></Link>)}</div></div></section></main>}
