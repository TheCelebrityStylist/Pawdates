import './globals.css';import type {Metadata} from 'next';import {Fraunces,Instrument_Sans,IBM_Plex_Mono} from 'next/font/google';import {Analytics} from '@vercel/analytics/react';import {canonicalHost} from '@/lib/seo';
// Passport register: a high-contrast serif masthead (Fraunces) + a machine-data
// monospace (IBM Plex Mono), with Instrument Sans as the neutral connective UI face.
const display=Fraunces({subsets:['latin'],weight:['600','700'],style:['normal','italic'],variable:'--font-display',display:'swap',fallback:['Georgia','serif'],adjustFontFallback:true});const body=Instrument_Sans({subsets:['latin'],weight:['400','500'],variable:'--font-body',display:'swap',fallback:['Arial','sans-serif'],adjustFontFallback:true});const mono=IBM_Plex_Mono({subsets:['latin'],weight:['400','500','700'],variable:'--font-mono',display:'swap',fallback:['Courier New','monospace'],adjustFontFallback:true});
export const metadata:Metadata={metadataBase:new URL(canonicalHost),title:{default:'Tailtend — pet treatment reminders that keep every date',template:'%s'},description:'Calm pet treatment reminders for flea, tick, worming, vaccinations and more.',alternates:{canonical:canonicalHost},openGraph:{siteName:'Tailtend',type:'website',images:['/opengraph-image']},twitter:{card:'summary_large_image',images:['/opengraph-image']}};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en" className={`${display.variable} ${body.variable} ${mono.variable}`}><body>{children}<Analytics/></body></html>}
