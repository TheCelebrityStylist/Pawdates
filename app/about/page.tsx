import Link from 'next/link';import {baseGraph,buildMetadata,canonicalHost,graphJson} from '@/lib/seo';import {founder,founderNode} from '@/lib/eeat';
export const metadata=buildMetadata({title:'About Tailtend',description:'Why Tailtend exists, who runs it, and how to reach us.',path:'/about'});
export default function About(){const path='/about';const fNode=founderNode();const graph=[...baseGraph(path),{'@type':'BreadcrumbList','@id':`${canonicalHost}${path}#breadcrumbs`,itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:canonicalHost},{'@type':'ListItem',position:2,name:'About',item:`${canonicalHost}${path}`}]},...(fNode?[fNode]:[])];
return <main className="min-h-screen bg-[var(--paper)]"><article className="mx-auto max-w-3xl px-5 py-20"><nav className="mono"><Link href="/">Home</Link> / <span>About</span></nav><h1 className="mt-10 text-5xl">About Tailtend</h1>
<section className="mt-10"><h2 className="text-3xl">Why Tailtend exists</h2><p className="muted mt-4">Pet-care dates are easy to lose track of: flea and worming treatments run on different intervals, vaccination boosters are spaced months apart, and the paper card or phone note that tracked them tends to disappear exactly when it&apos;s needed at a vet visit. Tailtend keeps one record per pet — what was done, when it&apos;s next due — and sends one calm reminder instead of relying on memory.</p></section>
<section className="mt-10"><h2 className="text-3xl">Who runs Tailtend</h2><p className="muted mt-4">Tailtend is built and operated by EAC Holding B.V.{founder?` Tailtend&apos;s ${founder.role.toLowerCase()} is ${founder.name}. ${founder.bio}`:''}</p><p className="muted mt-4">For privacy requests, support, or press enquiries, contact <a className="text-[var(--health)] underline" href="mailto:hello@tailtend.com">hello@tailtend.com</a>.</p></section>
<section className="mt-10"><h2 className="text-3xl">How we keep the guidance accurate</h2><p className="muted mt-4">Every schedule, product and life-stage guide on this site links to the published veterinary-guideline sources it draws on, in a references section at the bottom of the page. We&apos;re in the process of adding a named veterinary reviewer to sign off on new and updated guidance — see the <Link className="underline" href="/about/veterinary-reviewer">veterinary review page</Link> for the current status. None of this content is a substitute for advice from your own veterinarian, who can assess your specific pet.</p></section>
</article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(graphJson(graph))}}/></main>}
