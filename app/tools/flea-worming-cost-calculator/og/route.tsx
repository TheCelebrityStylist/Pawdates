import {ImageResponse} from 'next/og';import {annualCost,type ProductType,type WeightBand} from '@/lib/pet-cost';
export const runtime='edge';
export async function GET(request:Request){const {searchParams}=new URL(request.url);const species=searchParams.get('species')==='cat'?'cat':'dog';const band=(['small','medium','large'].includes(searchParams.get('band')||'')?searchParams.get('band'):'medium') as WeightBand;const product=(['flea','worming','both'].includes(searchParams.get('product')||'')?searchParams.get('product'):'both') as ProductType;const annual=annualCost(species,band,product);
return new ImageResponse(<div style={{width:'100%',height:'100%',display:'flex',flexDirection:'column',justifyContent:'space-between',background:'#F7F2E9',color:'#22303C',padding:70,fontFamily:'sans-serif'}}><div style={{fontFamily:'monospace',fontSize:24,letterSpacing:3,textTransform:'uppercase',color:'#2F7D5B'}}>Tailtend · Cost calculator</div><div style={{fontSize:72,fontWeight:800,lineHeight:1.05}}>{`EUR ${annual} / year`}</div><div style={{fontSize:32,marginTop:10}}>Estimated prevention cost</div><div style={{display:'flex',justifyContent:'space-between',borderTop:'2px solid rgba(34,48,60,.18)',paddingTop:25,fontFamily:'monospace',fontSize:22}}><span>vs. several times that if a treatment is missed</span><span>tailtend.com</span></div></div>,{width:1200,height:630})}
