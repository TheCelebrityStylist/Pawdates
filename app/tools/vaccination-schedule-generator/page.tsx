import Link from 'next/link';import {buildMetadata,canonicalHost,baseGraph,graphJson} from '@/lib/seo';import {Generator} from './generator';
const path='/tools/vaccination-schedule-generator';
export async function generateMetadata({searchParams}:{searchParams:Promise<Record<string,string>>}){const sp=await searchParams;const qs=new URLSearchParams(sp).toString();return buildMetadata({title:'Vaccination schedule generator for puppies and kittens',description:'Enter your puppy or kitten’s birth date and get a full personalised first-year vaccination schedule with real calendar dates.',path,image:`${path}/og${qs?`?${qs}`:''}`})}
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const sp=await searchParams;const graph=[...baseGraph(path),{'@type':'BreadcrumbList','@id':`${canonicalHost}${path}#breadcrumbs`,itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:canonicalHost},{'@type':'ListItem',position:2,name:'Tools',item:`${canonicalHost}/tools`},{'@type':'ListItem',position:3,name:'Vaccination schedule generator',item:`${canonicalHost}${path}`}]}];
return <main className="min-h-screen bg-[var(--paper)]"><article className="mx-auto max-w-2xl px-5 py-20"><nav className="mono"><Link href="/">Home</Link> / <Link href="/tools">Tools</Link></nav><h1 className="mt-10 text-5xl">Vaccination schedule generator</h1><p className="muted mt-5 text-xl">Enter your puppy or kitten&apos;s birth date and get real calendar dates for the whole first-year vaccination course.</p><Generator initial={{species:sp.species||'',birth:sp.birth||'',name:sp.name||''}}/></article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(graphJson(graph))}}/></main>}
