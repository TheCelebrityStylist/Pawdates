import Link from 'next/link';import {buildMetadata,canonicalHost,baseGraph,graphJson} from '@/lib/seo';import {Calculator} from './calculator';
const path='/tools/pet-age-calculator';
export async function generateMetadata({searchParams}:{searchParams:Promise<Record<string,string>>}){const sp=await searchParams;const qs=new URLSearchParams(sp).toString();return buildMetadata({title:'Pet age calculator — human years for dogs and cats',description:'A size-adjusted dog and cat age calculator using the real AVMA/AKC guideline, not the old ×7 rule.',path,image:`${path}/og${qs?`?${qs}`:''}`})}
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const sp=await searchParams;const graph=[...baseGraph(path),{'@type':'BreadcrumbList','@id':`${canonicalHost}${path}#breadcrumbs`,itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:canonicalHost},{'@type':'ListItem',position:2,name:'Tools',item:`${canonicalHost}/tools`},{'@type':'ListItem',position:3,name:'Pet age calculator',item:`${canonicalHost}${path}`}]}];
return <main className="min-h-screen bg-[var(--paper)]"><article className="mx-auto max-w-2xl px-5 py-20"><nav className="mono"><Link href="/">Home</Link> / <Link href="/tools">Tools</Link></nav><h1 className="mt-10 text-5xl">Pet age calculator</h1><p className="muted mt-5 text-xl">How old is your dog or cat really, in human years? Uses the real size-adjusted veterinary guideline, not the ×7 myth.</p><Calculator initial={{species:sp.species||'',size:sp.size||'',age:sp.age||'',name:sp.name||''}}/></article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(graphJson(graph))}}/></main>}
