import {ImageResponse} from 'next/og';import {humanYears,type DogSize} from '@/lib/pet-age';
export const runtime='edge';
export async function GET(request:Request){const {searchParams}=new URL(request.url);const species=searchParams.get('species')==='cat'?'cat':'dog';const size=(['small','medium','large','giant'].includes(searchParams.get('size')||'')?searchParams.get('size'):'medium') as DogSize;const age=Math.max(0,Math.min(30,Number(searchParams.get('age'))||0));const name=searchParams.get('name')||'Your pet';
const headline=age>0?`${name} is about ${humanYears(species,size,age)} in human years`:'How old is your pet in human years?';
return new ImageResponse(<div style={{width:'100%',height:'100%',display:'flex',flexDirection:'column',justifyContent:'space-between',background:'#F7F2E9',color:'#22303C',padding:70,fontFamily:'sans-serif'}}><div style={{fontFamily:'monospace',fontSize:24,letterSpacing:3,textTransform:'uppercase',color:'#2F7D5B'}}>Tailtend · Pet age calculator</div><div style={{fontSize:64,fontWeight:800,lineHeight:1.1,maxWidth:1050}}>{headline}</div><div style={{display:'flex',justifyContent:'space-between',borderTop:'2px solid rgba(34,48,60,.18)',paddingTop:25,fontFamily:'monospace',fontSize:22}}><span>Size-adjusted AVMA/AKC guideline</span><span>tailtend.com</span></div></div>,{width:1200,height:630})}
