import Link from 'next/link';import {buildMetadata} from '@/lib/seo';
const tools=[{slug:'is-my-pet-treatment-overdue',title:'Is it overdue?',blurb:'Enter the last date and get an instant overdue verdict.'},{slug:'vaccination-schedule-generator',title:'Vaccination schedule generator',blurb:'Birth date in, full first-year schedule out.'},{slug:'pet-age-calculator',title:'Pet age calculator',blurb:'The real size-adjusted formula, not ×7.'},{slug:'flea-worming-cost-calculator',title:'Flea & worming cost calculator',blurb:'What prevention costs — and what skipping it costs.'}];
export const metadata=buildMetadata({title:'Free pet care tools',description:'Free, no-signup tools for pet owners: an overdue checker, a vaccination schedule generator, an age calculator and a cost calculator.',path:'/tools'});
export default function Tools(){return <main className="min-h-screen bg-[var(--paper)]"><section className="section pt-20"><div className="wrap"><p className="mono text-[var(--health)]">Free tools</p><h1 className="mt-4 text-5xl">No signup. Just the answer.</h1><div className="mt-12 grid gap-4 md:grid-cols-2">{tools.map(t=><Link className="card p-6" href={`/tools/${t.slug}`} key={t.slug}><h3 className="text-xl">{t.title}</h3><p className="muted mt-2">{t.blurb}</p></Link>)}</div></div></section></main>}
