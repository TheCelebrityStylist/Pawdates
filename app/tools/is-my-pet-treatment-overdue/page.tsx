import Link from 'next/link';import {buildMetadata,canonicalHost,baseGraph,graphJson} from '@/lib/seo';import {Checker} from './checker';
const path='/tools/is-my-pet-treatment-overdue';
export async function generateMetadata({searchParams}:{searchParams:Promise<Record<string,string>>}){const sp=await searchParams;const qs=new URLSearchParams(sp).toString();return buildMetadata({title:'Is my pet’s treatment overdue?',description:'Enter the last date a treatment was given and get an instant overdue-or-not verdict, based on standard intervals.',path,image:`${path}/og${qs?`?${qs}`:''}`})}
export default async function Page({searchParams}:{searchParams:Promise<Record<string,string>>}){const sp=await searchParams;const graph=[...baseGraph(path),{'@type':'BreadcrumbList','@id':`${canonicalHost}${path}#breadcrumbs`,itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:canonicalHost},{'@type':'ListItem',position:2,name:'Tools',item:`${canonicalHost}/tools`},{'@type':'ListItem',position:3,name:'Is my pet’s treatment overdue?',item:`${canonicalHost}${path}`}]}];
return <main className="min-h-screen bg-[var(--paper)]"><article className="mx-auto max-w-2xl px-5 py-20"><nav className="mono"><Link href="/">Home</Link> / <Link href="/tools">Tools</Link></nav><h1 className="mt-10 text-5xl">Is it overdue?</h1><p className="muted mt-5 text-xl">Enter the last date and get an instant answer — overdue, due today, or due in N days.</p><Checker initial={{type:sp.type||'',last:sp.last||'',name:sp.name||'',species:sp.species||''}}/></article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(graphJson(graph))}}/></main>}
