import {ImageResponse} from 'next/og';import {checkOverdue} from '@/lib/overdue';
export const runtime='edge';
export async function GET(request:Request){const {searchParams}=new URL(request.url);const type=searchParams.get('type')||'flea_tick';const last=searchParams.get('last')||'';const verdict=last?checkOverdue(type,last):null;
const headline=!verdict?'Is your pet’s treatment overdue?':verdict.status==='overdue'?`Overdue by ${verdict.days} ${verdict.days===1?'day':'days'}`:verdict.status==='due-today'?'Due today':`Due in ${verdict.days} ${verdict.days===1?'day':'days'}`;
return new ImageResponse(<div style={{width:'100%',height:'100%',display:'flex',flexDirection:'column',justifyContent:'space-between',background:verdict?.status==='overdue'?'#F4E3DC':'#F7F2E9',color:'#22303C',padding:70,fontFamily:'sans-serif'}}><div style={{fontFamily:'monospace',fontSize:24,letterSpacing:3,textTransform:'uppercase',color:'#2F7D5B'}}>Tailtend · Overdue checker</div><div style={{fontSize:72,fontWeight:800,lineHeight:1.05,maxWidth:1050}}>{headline}</div><div style={{display:'flex',justifyContent:'space-between',borderTop:'2px solid rgba(34,48,60,.18)',paddingTop:25,fontFamily:'monospace',fontSize:22}}><span>Based on standard treatment intervals</span><span>tailtend.com</span></div></div>,{width:1200,height:630})}
