import {admin} from '@/lib/supabase';import {fail,ok} from '@/lib/http';import {serverEnv} from '@/lib/env';import {Resend} from 'resend';export async function GET(req:Request){try{const env=serverEnv();if(req.headers.get('authorization')!==`Bearer ${env.CRON_SECRET}`)return fail('unauthorized','Invalid cron secret',401);const db=admin();const today=new Date().toISOString().slice(0,10);const {data,error}=await db.rpc('due_reminder_digest',{p_today:today});if(error)return fail('query_failed',error.message,500);let sent=0;for(const row of data||[]){const key=`${row.user_id}:${today}:digest`;const {error:dedupe}=await db.from('email_events').insert({user_id:row.user_id,type:'digest',dedupe_key:key,payload:{count:row.items.length}});if(dedupe)continue;const plural=row.items.length>1;const first=row.items[0];const subject=plural?`${row.items.length} treatments coming up for your pets`:`${first.pet_name}'s ${first.name.toLowerCase()} is due ${first.days===0?'today':`in ${first.days} days`}`;const lines=row.items.map((x:{pet_name:string;name:string;product_name?:string;days:number})=>`${x.pet_name}: ${x.name}${x.product_name?` (${x.product_name})`:''} — ${x.days<0?'2 days overdue':x.days===0?'due today':`due in ${x.days} days`}`).join('\n');const r=await new Resend(env.RESEND_API_KEY).emails.send({from:'PawDates <reminders@pawdates.app>',to:row.email,subject,text:`${lines}\n\nOpen PawDates: ${env.NEXT_PUBLIC_APP_URL}/app\n\nUnsubscribe in PawDates settings.`,headers:{'List-Unsubscribe':`<${env.NEXT_PUBLIC_APP_URL}/app/settings>`}});if(r.error)await db.from('email_events').delete().eq('dedupe_key',key);else sent++}return ok({sent})}catch(e){return fail('reminder_failed',e instanceof Error?e.message:'Reminder job failed',500)}}
