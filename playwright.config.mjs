import {defineConfig} from '@playwright/test';const baseURL=process.env.PLAYWRIGHT_BASE_URL;if(!baseURL||/localhost|127\.0\.0\.1/.test(baseURL))throw new Error('PLAYWRIGHT_BASE_URL must be a deployed preview URL, never localhost');export default defineConfig({testDir:'./tests/e2e',timeout:90000,retries:1,use:{baseURL,channel:'chrome',trace:'on',video:'on',screenshot:'only-on-failure'},reporter:[['list'],['html',{outputFolder:'design-review/playwright-report',open:'never'}]],outputDir:'design-review/test-results'});
