import {defineConfig} from '@playwright/test';const baseURL=process.env.PLAYWRIGHT_BASE_URL;const bypass=process.env.VERCEL_AUTOMATION_BYPASS_SECRET;if(!baseURL||/localhost|127\.0\.0\.1/.test(baseURL))throw new Error('PLAYWRIGHT_BASE_URL must be a deployed preview URL, never localhost');if(baseURL.includes('.vercel.app')&&!bypass)throw new Error('VERCEL_AUTOMATION_BYPASS_SECRET is required for the protected preview');export default defineConfig({testDir:'./tests/e2e',timeout:90000,retries:1,use:{baseURL,channel:'chrome',extraHTTPHeaders:bypass?{'x-vercel-protection-bypass':bypass,'x-vercel-set-bypass-cookie':'true'}:{},trace:'on',video:'on',screenshot:'only-on-failure'},reporter:[['list'],['html',{outputFolder:'design-review/playwright-report',open:'never'}]],outputDir:'design-review/test-results'});
