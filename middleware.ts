import {createServerClient,type CookieOptions} from '@supabase/ssr';import {NextResponse,type NextRequest} from 'next/server';type CookieWrite={name:string;value:string;options:CookieOptions};export async function middleware(req:NextRequest){let res=NextResponse.next({request:req});const s=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,{cookies:{getAll:()=>req.cookies.getAll(),setAll(items:CookieWrite[]){items.forEach(x=>req.cookies.set(x.name,x.value));res=NextResponse.next({request:req});items.forEach(x=>res.cookies.set(x.name,x.value,x.options))}}});await s.auth.getUser();return res}export const config={matcher:['/app/:path*']};
