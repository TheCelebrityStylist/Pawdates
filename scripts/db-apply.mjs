import fs from 'node:fs';import pg from 'pg';const url=process.env.SUPABASE_DB_URL;if(!url){console.error('SUPABASE_DB_URL is required');process.exit(2)}const file=process.argv[2]||'supabase/migrations/0004_consolidated_ship.sql';const sql=fs.readFileSync(file,'utf8');const client=new pg.Client({connectionString:url,ssl:{rejectUnauthorized:false}});try{await client.connect();await client.query(sql);console.log(`Applied ${file}`);await client.query("NOTIFY pgrst, 'reload schema'");console.log('PostgREST schema reload notified')}catch(error){console.error('Migration failed:',error.message);process.exitCode=1}finally{await client.end()}
